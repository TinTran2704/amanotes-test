# Copy this to .env (or export in your shell) before running with metadata DB.
# Skip entirely if running daily_metrics.py with --no-db.

PGHOST=localhost
PGPORT=5432
PGDATABASE=amanotes
PGUSER=postgres
PGPASSWORD=postgres
